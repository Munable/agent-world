# 仓库工作约定

先读 [产品定义](PRODUCT_POSITIONING.md) 与 [分层基线](docs/BASELINE.md)，再读受改动影响的能力合同。
本文件约束仓库维护方式，不新增产品原则、通用玩法或审批角色。

## 改动边界

- 产品目标以 PRODUCT_POSITIONING 为准。实现缺口不能反过来改写目标；产品目标变更需要项目负责人明确决定。
- 原则、领域模型、协议合同、实现是四种说明层级，不是四个强制目录，也不是必须依次完成的开发阶段。
- 测试、UI 验收和运行证据跨层使用。实验可以否定假设，但不能默默重新定义授权、确认或业务完成。
- 未决机制只在 docs/OPEN_DESIGN.md 维护。第三方／群众公裁仍待设计；不要替项目选定票数、资格或裁决规则。
- 先检查分支、提交、未提交改动及差异。不要用旧文件整块覆盖当前文件；不擅自清理真实数据库、凭据、其他任务进程或用户工作区。
- 内核／SDK 不依赖 HTTP、MCP、UI 或具体世界。组合入口、加载器、内置示例可以依赖这些模块；不强迫整个仓库形成单向导入链。
- 根目录的旧模块是兼容入口，旧脚本是回归资产；移除或改名需说明消费者影响，不能只为目录整齐。

## 验证与记录

行为修复先留下能复现问题的测试。删除／弱化既有回归必须说明对应合同为何失效或由什么等价测试替代；测试数量下降应核对原因。
同一次改动只记录一次：改了哪层、依据是什么、影响什么、实际验证及未验证项。普通实现细节不需要更新所有上层文档。
本地默认运行 `python tools/run_tests.py`；客户端变更运行两份 Node 测试，发布／包边界变更运行 `python tools/check_package.py`。
纯文档变更检查内容、相对链接和 diff，不反复运行完整计费 CI。实际测试命令与日志保存在本次审计证据中。
不得把未执行、跳过、失败或环境受阻写成通过，也不得把固定脚本称作自主 Agent 或长期运行证明。

本次整理依据与范围：[2026-09-26 审计](docs/REPOSITORY_AUDIT_2026-09-26.md)。
